# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
/android/build/

# Local run helper (contains anon key)
run_on_phone.ps1
build_apk_for_phone.ps1

# MVP dev-auth helpers + secret (bypass login; never commit)
run_dev_mvp.ps1
build_apk_dev_mvp.ps1
build_appbundle_playstore.ps1
.dev_auth_secret.txt

# Contains the Supabase personal access token
.env

# Per-environment dart-define files (contain real keys). Only example.json is tracked.
/config/*.json
!/config/example.json

# Real center/coordinator data (contains personal phone numbers). Never commit.
Gachibowli Center Details for Siddhu - Gachibowli Subcenters.csv
*Gachibowli Subcenters*.csv
Gachibowli Center Details for Siddhu - Preceptor Details.csv
*Preceptor Details*.csv